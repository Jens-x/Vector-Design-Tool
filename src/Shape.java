/**
 * abstract class the defines some of the fields and methods that every concrete shape class must have
 */
abstract class Shape {
    int x1,y1;

    abstract int getX1();
    abstract int getY1();
}
